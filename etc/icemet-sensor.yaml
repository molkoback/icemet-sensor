# ICEMET Sensor config
#

# Files
save_path: "~/.icemet"
save_type: "png"

# Sensor settings
sensor_id: 1
sensor_restart: -1

# Cameras: spin{id}, pylon{}
camera: {"spin": {
"id": 0
}}

# Lasers: picolas{port}, icemet{}
laser: {"icemet": {}}

# FTP settings
ftp_host: "localhost"
ftp_port: 21
ftp_user: "icemet"
ftp_passwd: ""
ftp_path: "."

# Measurement settings
measure_fps: 1
measure_len: 60
